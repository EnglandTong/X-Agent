node_modules/
dist/
*.db
*.db-journal
.DS_Store

# secrets / local config — never commit
.env
.env.local

# delivery snapshots — regenerate after milestones; not source of truth
Agent_ERP.bundle
agt-erp-src.zip
*.bundle

# eval run artifacts (keep utterances + failures.jsonl committed)
app/eval/results/

# large model weights (see app/models/OFFLINE_BUNDLE.md)
*.onnx
*.gguf
*.bin
*.pt
*.safetensors
app/models/asr/sensevoice/**
!app/models/asr/sensevoice/.gitkeep
!app/models/asr/sensevoice/README*
app/models/llm/**/*.gguf
app/models/llm/**/*.bin
