node_modules/
dist/
*.db
*.db-journal
.DS_Store

# secrets / local config — never commit
.env
.env.local

# delivery snapshots — regenerate after milestones; not source of truth
Agent_ERP.bundle
agt-erp-src.zip
*.bundle
