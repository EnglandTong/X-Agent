# AGT-ERP 规格说明书
## Agent 原生 ERP · 销售订单 → 出货线（全新开发）

| 项 | 值 |
|---|---|
| 版本 | v0.4（**W0 骨架完成，Schema 双消费架构已在真实代码上验证**） |
| 状态 | **W0 已交付，可在线体验** |
| 参考对象 | BW_ERP（**仅参考其表单格式与业务流程，不复用代码、不改其代码**） |
| 开发方式 | **Greenfield，独立新应用** |
| 本期范围 | **销售订单 → 出货，一条线（不多一个模块）** |
| _owner | Owner |

---

## 0. 变更说明（v0.1 → v0.4）

| 版本 | 方案 | 结果 |
|---|---|---|
| v0.1 | 在 BW_ERP 上外挂 Sidecar 网关 | 已废弃 |
| v0.2 | 同上，Agent 循环改用 Pi SDK | 已废弃 |
| v0.3 | 全新独立应用，BW_ERP 仅作参考 | 已交付 |
| **v0.4** | **W0 骨架落地：Schema 双消费 + 消解器 + 动词层 + 手机端原型** | **当前状态** |

### v0.4 新增：W0 验证证据

架构不再是纸面设计，以下代码已在真实运行并被验证：

| 验证项 | 结果 | 证据 |
|---|---|---|
| Formily 能否用 JSON Schema 渲染表单 | **✅ 8 个字段全部渲染，零手写表单代码** | 浏览器实测字段数 = 8，控制台零错误 |
| 编译器能否拦截错误配置 | **✅ 拦截"声明 enum 消解却没给 enum 列表"** | 该 bug 在开发中真实发生并被补进校验规则 |
| 消解器能否把口语转成 ID | **✅ "张三"→客户 ID、"下周三"→2026-09-09、"120"→120** | 端到端实测 |
| 歧义能否不瞎猜 | **✅ "张"→弹张三/张伟候选，不自动选中** | 歧义分差 < 0.08 时返回 candidates |
| 业务规则能否拦截 | **✅ 信用超额 block、起订额 block、价格底线 confirm、库存 warn** | 四类规则均实测触发 |
| 推断值能否被看见 | **✅ 确认页三色标注：你说的(绿)/推断(黄)/待补(红)** | 浏览器实测标签分布正确 |

**变更带来的三个解放：**

1. 动词层从"外挂网关"升级为"应用地基" —— agent-native 才是真原生
2. 前端选型自由 —— 可避开 TDesign 适配成本（见 2.1）
3. 无需维护两套系统（宿主 + 网关）

**同时带来一个必须严防的风险：** "反正都是新的"是范围蔓延最强的诱饵。**范围一个字不变**，见 STOP_RULES 第 1 条。

---

## 1. TARGET

### 1.1 一句话目标

新建一个**原生以 Schema 与业务动词为地基**的小型应用，让使用者用一句话（未来是语音）完成"建销售订单 → 转出货单"，最终由人在表单上确认。

**BW_ERP 的角色：参考资料。** 照抄它的字段定义、校验规则、流程顺序、命名习惯；**不复用它的一行代码，也不改动它一行代码。**

### 1.2 范围边界

**本期做（销售订单 → 出货一条线）：**

```
用户一句话 / 语音
    ↓
[Agent 层] 意图识别 → 槽位抽取 → 查库匹配 → 歧义消解 → 缺槽反问
    ↓
[确认页] Schema 渲染的表单，推断项高亮
    ↓
人确认 / 修改
    ↓
[动词层] 执行业务动词 → 落库 + 全程留痕
```

**本期明确不做（钉死，不因"全新"而松动）：**

- ❌ 采购、入库、库存调拨、财务过账
- ❌ 审批流（这是流程问题，不是模型问题）
- ❌ 多订单合并、拆单、冲销
- ❌ 批量改价、特批
- ❌ 「把 BW_ERP 的其他模块也重做一遍」
- ❌ 通用报表引擎、打印模板引擎、导入导出框架
- ❌ 语音输入（W4 再接）

> ⚠️ **本项目不是"重写 ERP"。** 它是一个只覆盖「订单 → 出货」这条线的小型应用。凡是不在这条线上的，一律不做。

### 1.3 成功判据（三个月后回头看）

| 指标 | 目标值 |
|---|---|
| 建单耗时 | 3 分钟 → 45 秒内 |
| 槽位抽取准确率（微调后） | ≥ 85% |
| 本地闭环率（不调云端） | ≥ 80% |
| 事故数 | 0（写入必须经确认页） |
| **范围蔓延次数** | **0**（见 STOP_RULES） |

---

## 2. 核心技术决策

### 2.1 决策表

| 层 | 选型 | 理由 |
|---|---|---|
| 表单协议 | **Formily JSON Schema** | Schema 是真源；UI 与 AI 工具都是它的消费者 |
| 前端框架 | **React 19 + Vite + TypeScript** | Formily 对 React 支持最成熟 |
| UI 组件库 | **Ant Design** | **Formily 官方适配**，省掉 TDesign 适配层（约 2-3 周工作量） |
| 后端 | **Fastify + TypeScript** | 轻量、快、Owner 熟悉 |
| ORM / 数据库 | **Prisma + PostgreSQL** | ERP 需事务、关系、查询能力；Postgres 强于 MongoDB |
| Agent 循环 | **Pi SDK**（`@earendil-works/pi-coding-agent`） | 见 2.2 |
| 本地模型 | **星火 X2.5-1.7B**（Ollama，Q4） | 1.2GB，RTX 4050 6GB 流畅 |
| 云端升级 | **火山引擎 Ark / DeepSeek** | 已有配置，仅低置信度时调用 |
| ASR | **讯飞语音听写** | W4 再接 |

**关于 UI 库的重要说明：** 原方案受限于 BW_ERP 的 TDesign 栈，需自写 Formily 适配层。改为全新开发后，可直接采用 Formily 官方支持的 Ant Design，**省下 2-3 周且生态最全**。若 Owner 坚持 TDesign，需追加适配工作量，但架构不变。

### 2.2 为什么选 Pi 而不是 DSH

> 完整对比见 v0.2 记录，此处保留结论。

| 维度 | DSH | **Pi** |
|---|---|---|
| 首次发布 | 2026-08-13（24 天） | 2025-08-09（**13 个月**） |
| 版本 | v0.1.0-rc.5 | **v0.84.2** |
| Stars | 1.5k | **77.8k** |
| 系统提示 | 未公开 | **< 1000 token** |
| Provider | 支持 | **15+，含 Ollama** |
| SDK 嵌入 | 有 | 有（OpenClaw 建于 Pi 之上） |
| Windows | ACL 受限令牌（最弱） | ✅ 原生支持 |
| 沙箱 / 权限 | 有 | ❌ **无内置** |

**三个契合点：**

1. **SDK 模式嵌入** —— 作为 Node 包嵌入 Fastify，**不用 TUI**，是官方支持路径
2. **lazy skills 天然省成本** —— 技能只留一行描述常驻上下文，调用时才加载完整指令，直接解决前缀成本（97% 命中 $0.032 vs 全价 $0.53，差 16 倍）
3. **树状会话可分支回放** —— 满足审计留痕需求

**必须补偿的风险：Pi 无内置沙箱与权限系统（第三方评测安全项 5/10）。**

| 风险 | 补偿 |
|---|---|
| 无沙箱 | Agent 只调应用内部服务层，**不碰文件系统、不执行 shell** |
| 无权限系统 | 权限校验在领域层实现，AI 使用操作者身份 |
| 写入风险 | 所有写入必经确认页 |
| 工具越界 | **禁用 Pi 内置 read / write / edit / bash** |

> ⚠️ **禁用内置文件与 shell 工具是硬性要求**，已写入 STOP_RULES。

**安装注意：** 2026-05 后 npm scope 从 `@mariozechner` 迁至 `@earendil-works`，旧教程的包名已废弃。

### 2.3 原生分层架构（不再是 Sidecar）

```
┌───────────────────────────────────────────────┐
│  L1 交互层                                     │
│   ├─ 自然语言输入框（一句话建单）               │
│   ├─ Schema 渲染的表单（点按钮也能用）          │
│   └─ 确认页（推断项高亮 + 可改任意字段）        │
├───────────────────────────────────────────────┤
│  L2 Schema 层 ← 唯一真源                       │
│   └─ Formily JSON Schema + x-agent 扩展        │
│       ├─ 消费方 A：UI 渲染器                   │
│       └─ 消费方 B：Tool 编译器 → LLM 工具定义   │
├───────────────────────────────────────────────┤
│  L3 Agent 层（Pi SDK，可替换）                  │
│   ├─ 意图识别 / 槽位抽取 / 消解 / 反问          │
│   ├─ 本地星火 1.7B（Ollama）                   │
│   └─ 云端升级通道（低置信度）                   │
├───────────────────────────────────────────────┤
│  L4 动词层 ← 一等公民，不是外挂                 │
│   └─ 12 个业务动词（见第 4 节）                 │
│       --json / --dry-run / --schema / 幂等     │
├───────────────────────────────────────────────┤
│  L5 领域层                                     │
│   ├─ Prisma + PostgreSQL                       │
│   ├─ 业务规则：信用 / 库存 / 价格 / 权限        │
│   └─ 审计表（谁、原话、改了什么）               │
└───────────────────────────────────────────────┘
```

**关键约束：**

1. **L2 Schema 是唯一真源** —— 表单和 AI 工具都由它生成，改字段只改一处
2. **L4 动词层是一等公民** —— UI 和 Agent 都通过它操作业务，不存在"绕过动词直接改库"
3. **L3 可替换** —— Agent 层换掉不影响 L4/L5

### 2.4 与 BW_ERP 的关系（需 Owner 决策）

| 方案 | 说明 | 优劣 |
|---|---|---|
| **A. 完全独立**（推荐） | 新库、新数据，与 BW_ERP 零耦合 | ✅ 最安全，互不干扰<br>❌ 双系统并存期需双录 |
| B. 共享数据库 | 新旧系统操作同一份数据 | ✅ 无数据割裂<br>❌ 与接近上线的系统抢同一份数据，风险高 |
| C. 只读 BW_ERP 库 | 新系统读旧库，写自己的 | 折中，但耦合仍在 |

**推荐 A。** BW_ERP 接近上线，此时共享数据库等于给两个系统制造耦合。独立跑通后，再决定是迁移还是并存。

---

### 2.5 画布模型：ERP 的 REPL（Owner 决策，v0.4 新增）

**界面不是一堆预先写好的页面，而是一块画布 + 一条常驻命令输入。**

```
┌──────────────────────────┐
│ 当前对象：SO-2026-1007    │ ← 跨格子共享上下文
├──────────────────────────┤
│  #1 order.create  只读快照 │ ← 提交即冻结
│  #2 order.create  活动中   │ ← 永远最多一个
├──────────────────────────┤
│ › 说一句话…               │ ← 命令输入，永远在底部
└──────────────────────────┘
```

底部输入 = 命令行；每次调用动词 = 画布 Print 一个格子。**这不是"AI 生成版面"，是从 Schema 库调取已固定的格式现场实例化**——版式预先钉死，只是现在才渲染，所以没有"慢 / 每次不一样 / 不可控"三个坑。

#### 铁律一：格子提交即冻结，永不原地修改

| 动作 | 系统行为 |
|---|---|
| 提交 | 格子落库，状态 `SUBMITTED`，**无任何编辑入口** |
| 要改 | **新开一格**，`supersedesId` 指向旧格，旧格标记 `SUPERSEDED` 并原地冻结 |
| 回看 | 旧格内容一字未动，完整可查 |

#### 铁律二：订单确认后不可改，要改就另开一张单

> **这是 Owner 纠正过的关键区分。** 第一版做成了「同一张单的版本覆盖」（单号不变、内容替换），
> 这是错的。正确语义是：**确认后的单纹丝不动，另开一张新单，用业务关键字段串联。**

| 原单状态 | 动作 | 结果 |
|---|---|---|
| `DRAFT` 草稿 | **修订** | 同一张单原地更新，单号不变 |
| `CONFIRMED` 已确认 | **变更** | **另开新单**（新单号），原单冻结为 `SUPERSEDED` |
| `SHIPPED` 已出货 | **变更** | 同上 |
| `SUPERSEDED` 已变更 | — | **拒绝**：已被 X 变更过，请对最新的那张单操作 |

于是「改一张已确认的单」在库里的样子 —— **两张单各自独立存在，谁都没被改写**：

```
SO-2026-1008  SUPERSEDED  ¥4,500   originNo=—            supersededByNo=SO-2026-1009
SO-2026-1009  DRAFT       ¥13,500  originNo=SO-2026-1008  supersededByNo=—
```

串联靠的是**业务关键字段**，不是技术 ID：`originNo`（新单指向原单）+ `supersededByNo`（原单指回新单）双向互指，
`chainId`（变更链根单号）让整条链共享一个追溯入口。

#### 关联：主关联键 + 词典关键词

- **correlationId**：主关联键，把同一业务对象的所有操作串成一条线。优先级 **订单 > 客户**，如 `ORD:SO-2026-1007`
- **entities**：从主数据词典识别出的实体（张三 / A-100 / 华东仓 / SO-2026-1007），既是可检索的关键词，也是画布上可见的连接线索

> Owner 原话："在两件单子收过来的时候，前一张跟后一张有一些关键的信息去串联那个"。
> 即：**关联的是「两张单」，不是「一张单的两个版本」**，串联靠业务关键字段（单号），不是技术主键。

#### 领域层底线

不可变是对**格子**的约束，不等于领域对象能随便改。动词层仍要守住业务规则：

> **草稿可原地改；已确认 / 已出货必须另开变更单；已变更的单不能再变更。**

`order.confirm` 因此成为整条链上最关键的门槛 —— 没有它，所有单永远停留在草稿，变更单机制形同虚设。

#### 三个顺手拿到的红利

1. **审计天然成立** —— 不需要额外的操作日志表，画布本身就是
2. **训练数据天然成立** —— 每格都带着「原话 → 抽取 → 落库」三元组，W1 的样本采集口子直接打开
3. **撤销天然成立** —— 回滚就是让上一格重新成为当前版本

---

## 3. 关键设计：x-agent 扩展协议

在标准 Formily Schema 上扩展 `x-agent` 字段，**这是"一份 Schema 两个消费者"的落地方式**。

| 字段 | 消费者 | 用途 |
|---|---|---|
| `title` | UI + AI | 表单标签 / 工具参数描述（**白送的描述**） |
| `type` | UI + AI | 控件类型 / JSON Schema 类型 |
| `required` | UI + AI | 必填校验 / 必填参数 |
| `enum` / `minimum` | UI + AI | 选项约束 / 参数约束 |
| `x-component` | UI | 渲染什么组件 |
| `x-agent.extract` | AI | 从自然语言抽取的槽位名 |
| `x-agent.examples` | AI | few-shot 示例，喂给小模型 |
| `x-agent.resolution` | AI | 消解策略：`fuzzy_customer` / `exact` / `enum` / `date_parse` |
| `x-agent.askIfMissing` | AI | 缺失时是否反问 |
| `x-agent.inferFrom` | AI | 推断来源（如按历史订单推断仓库） |

示例见 `schema/order.create.json`，编译产物见 `schema/order.create.tool.json`，编译器见 `tools/schema-to-tool.ts`（**已验证可运行**）。

---

## 4. 动词清单（12 个，分三批）

### 第一批 · W1-W2

| 动词 | 类型 | 说明 |
|---|---|---|
| `order.query` | 🟢 只读 | 查订单。**先做它**：零风险，验证整条链路 |
| `order.create` | 🟡 写入 | 建销售订单，内含 credit / inventory 预检 |
| `credit.check` | 🟢 只读 | 信用额度检查（供 create 内部调用） |
| `inventory.availability` | 🟢 只读 | 库存可用性检查（供 create 内部调用） |

### 第二批 · W3

| 动词 | 类型 | 说明 |
|---|---|---|
| `shipment.createFromOrder` | 🟡 写入 | 订单转出货单（固定转换，高价值） |
| `order.confirm` | 🟡 写入 | 订单确认 |
| `order.listByCustomer` | 🟢 只读 | 按客户查历史订单 |
| `inventory.reserve` | 🔴 写入 | 库存占用（需双重确认） |

### 第三批 · W4+

| 动词 | 类型 | 说明 |
|---|---|---|
| `shipment.pick` | 🔴 写入 | 拣货 |
| `shipment.ship` | 🔴 写入 | 出库发货 |
| `shipment.track` | 🟢 只读 | 物流跟踪 |
| `order.cancel` | 🔴 写入 | 取消订单 |

### CLI 五原则（所有动词遵守）

1. **少而稳** —— 12 个动词，不是 200 个 endpoint
2. **结构化输出** —— 统一 `--json`
3. **`--dry-run`** —— 写入类必须有，先返回"会改什么"
4. **可自省** —— `erp schema order.create`
5. **错误可恢复** —— 返回候选而非仅报错：`{error:"customer_ambiguous", candidates:[...]}`
6. **幂等** —— 带 idempotency key，防网络重传重复建单

---

## 5. 里程碑

| 阶段 | 周期 | 交付 | 验收 | 状态 |
|---|---|---|---|---|
| **W0** | 3-5 天 | 项目骨架 + Schema 双消费 + 消解器 + 2 个动词 + 手机端原型 | 端到端跑通 | **✅ 已完成** |
| **W1** | 2 周 | Pi SDK 接入 + `order.query` 换真实模型 | 自然语言查单准确率 ≥ 80% | 进行中 |
| **W2** | 1 个月 | `order.create` + 确认页（推断项高亮） | 端到端建单成功，0 事故 |
| **W3** | 2-3 个月 | 攒样本 + LoRA 微调 1.7B；`shipment.createFromOrder` | 抽取准确率 ≥ 85% |
| **W4** | 6 个月 | 接讯飞 ASR + 型号词典 | 语音建单可用 |

### 5.1 W0 落地教训（写下来是为了 W1 不再踩）

这六条都是开发中真实踩到的，不是理论推演。

| # | 教训 | 具体事故 | 已落地的规则 |
|---|---|---|---|
| 1 | **命名双轨是最大的坑** | Schema 里同时存在**字段名**（`customerId`，UI 与落库用）和**槽位名**（`customer`，模型与意图层用）。执行校验拿槽位名去查字段名参数，导致 `order.create` 全部返回 400 | 跨层传递时必须显式声明用的是哪套命名；执行层校验一律用**原始 Schema 的 required**（字段名） |
| 2 | **编译器必须拦配置错误** | `warehouseId` 声明了 `resolution: enum` 却没写 enum 列表 → 任何仓库输入都被判非法，用户只看到"华北仓 不是有效的选项"且无候选可选 | 编译期校验：声明 enum 消解但缺 enum 列表 → 直接报错，不许上线 |
| 3 | **推断必须有前置条件** | 客户还处于歧义未决时，系统照样吐出"该客户无历史成交价，暂取牌价"——客户是谁都不知道，这句话纯属编造 | `inferFrom` 依赖的槽位未确定时，跳过推断并把目标槽位降级为 missing |
| 4 | **主数据词典不是 hack，是必需品** | 纯正则抽不到"查一下张三最近订单"里的张三（没有"给/客户"前缀） | 抽取层永远要有词典兜底。真实系统里模型抽完后同样要用词典做**实体链接**校正（"张总"→"张三"） |
| 5 | **非必填缺失不能标红** | 把"可选项没填"渲染成红色错误，用户会以为系统坏了 | `required` 标记必须从 Schema 一路传到 UI；非必填缺失标灰"留空" |
| 6 | **remark 兜底字段要防贪婪** | 早期版本把"C-300 20个"整段吃进备注，产品信息被糟蹋 | 只认明确引导词（备注/注意/记得/另外），且内容不得与已抽取槽位重复 |

---

## 6. WORK_ORDER · W1

```yaml
id: W1
title: Schema 注册表 + Tool 编译器 + Pi 接入 + order.query 打通
type: Standard
priority: P0

scope_in:
  - 用 Designable 拖出 order.query / order.create 表单，导出 JSON Schema
  - 接入 Pi SDK（禁用内置 read/write/edit/bash，仅注册 order.query）
  - 实现消解器：fuzzy_customer / exact / enum / date_parse
  - 接入本地星火 1.7B（Ollama）
  - 打通 order.query 全链路

scope_out:
  - 语音输入
  - 微调（W3）
  - TDesign（已改用 antd）
  - 任何写入类动词
  - DSH 集成（已改用 Pi）
  - Pi 的 TUI / 交互式用法（只用 SDK 模式）
  - BW_ERP 的任何代码复用或改动

dependencies:
  - W0: 项目骨架
  - BW_ERP 的字段与流程参考文档（只读参考）

acceptance:
  - 输入「查一下张三上个月的单」→ 返回正确订单列表
  - 客户名模糊（"张"）→ 弹候选，不自己拍板
  - 缺参数 → 一次问完，不挤牙膏
  - 20 条测试语句，意图识别准确率 ≥ 80%
  - Pi 内置工具确已禁用（代码审查）

stop_rules:
  - 不复用 BW_ERP 任何代码
  - 不新增任何写入类动词
  - 不接语音
  - 超过 3 周未完成 → 停止并复盘范围
```

---

## 7. ACCEPTANCE（全局验收标准）

| # | 标准 | 判定方式 |
|---|---|---|
| A1 | 所有写入必经确认页 | 代码审查 + 抓包 |
| A2 | AI 操作使用操作者身份，权限校验在领域层 | 用无权限账号尝试，必须被拦 |
| A3 | 歧义必须弹候选，不自动拍板 | 造 3 个重名客户测试 |
| A4 | 确认页标注每个字段来源（说的 / 推断的 / 默认的） | UI 验收 |
| A5 | 每次 AI 操作留痕：谁、原话、改了什么 | 查审计表 |
| A6 | Schema 改一处，UI 与工具定义同步生效 | 改字段后两边同时变化 |
| A7 | 本地闭环率 ≥ 80% | 统计云端调用占比 |
| A8 | Pi 内置文件与 shell 工具确已禁用 | 代码审查 |
| A9 | **未新增任何订单→出货以外的模块** | 范围审计 |

---

## 8. STOP_RULES（防止范围蔓延）

> 此节针对 Owner 历史痛点（8+ 项目烂尾）设立。**改为全新开发后，此节重要性上升为最高。**

1. ⛔ **本项目不是重写 ERP。** 只做「销售订单 → 出货」一条线。任何不在这条线上的模块，一律不做
2. ⛔ **不复用、不改动 BW_ERP 的任何代码**，它只是参考资料
3. ⛔ **只用 Pi，且必须禁用其内置 read / write / edit / bash**
4. ⛔ **不接语音**（W4 前）
5. ⛔ **不做通用引擎** —— 报表引擎、打印引擎、导入导出框架、工作流引擎，一律不做
6. ⛔ **不追求模型能力上限**，1.7B 够用就不换大的
7. ⛔ **单个工单超期 50% → 停止并复盘范围，不硬扛**
8. ⛔ **任何"顺手优化"一律拒绝**，记入 backlog

**蔓延自检清单（每次开工前过一遍）：**

- [ ] 我正在做的东西，在「订单 → 出货」这条线上吗？
- [ ] 如果不在，是不是因为"反正都是新的，顺便做了"？
- [ ] 如果是 → 停下，记入 backlog

---

## 9. 待确认项（需 Owner 补充）

| # | 项 | 影响 | 建议 |
|---|---|---|---|
| **Q1** | 与 BW_ERP 的数据关系？ | 决定数据库设计 | **建议 A：完全独立** |
| **Q2** | 建一张销售订单，必填字段有几个？ | 决定 OrderSchema 复杂度 | 参考 BW_ERP 现有表单 |
| **Q3** | 出货环节现在的具体步骤？ | 决定 W3 的动词粒度 | 参考 BW_ERP 流程 |
| **Q4** | UI 库接受 Ant Design 吗？ | 若坚持 TDesign 需 +2-3 周适配 | **建议 antd** |
| **Q5** | 目标用户是自己试用，还是要给别人用？ | 决定权限体系的工作量 | 自用可先简化 |

---

## 10. 附：为什么把范围钉这么死

Owner 已有 8+ 项目烂尾于范围蔓延，且手中有一个接近上线的 BW_ERP。全新开发是最诱人也最危险的起点——**"反正都是新的"会让人忍不住把一切都重做一遍。**

本规格的全部设计围绕一个原则：

> **范围与旧方案完全一致（订单 → 出货一条线），只是地基换成新的。**

这样即使新项目中途停止，损失也可控；而一旦跑通，它就是 BW_ERP 未来演进的参考实现。
