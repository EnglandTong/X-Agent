# 2026-09-06 · 只读通读系统 + 核对落地计划

## 结论：工作树已领先 git 最后一次提交（f1eaa81）959 行，全部未提交
- 已跟踪改动 19 个文件，未跟踪新文件 6 个（11_PERSONAL_LEXICON.md、lexicon.ts、remaining.ts、
  eval/utterances.lexicon.jsonl、scripts/eval-lexicon.ts、scripts/eval-compare-models.ts）。
- 落地计划的 阶段0 / 阶段1（1.1–1.6）/ 阶段2-P0 / 阶段4脚本 **代码都已写完**，但**未跑验收、未提交**。

## 实测对照（计划项 → 代码位置）
- 阶段0 检查清单：`HANDOVER/06_ROADMAP.md:29` 已有「加能力检查清单」+ 开工三问 + 验收表。
- 1.1 PersonalLexeme：`app/prisma/schema.prisma:106`；dev.db 已建表（0 行）。
- 1.2 lexicon.ts + 5 条路由：`src/server/lexicon.ts`(336) / `index.ts:152-228`。
- 1.3 插入点B（slot）：`resolve.ts:370-410`，fuzzy/enum 前查用语，失效自动 retire。
- 1.5 插入点A（verb）：`agent.ts:33,373-385`，detectVerb 前命中 conf 0.95。
- 1.4 确认卡记住：`ConfirmCard.tsx:170-203`。
- 1.6 确认后提议：`App.tsx:283-299` + `562-632` 提示条。
- 2-P0 剩余量/禁止超量：`src/server/remaining.ts` + delivery.create.ts:39-67 + delivery.confirm.ts:34-45。
- 阶段4 对比脚本：`scripts/eval-compare-models.ts`（LOCAL_LLM_* 环境变量）。
- 2-P1 code 消解收紧：**未做**。阶段3 真链路试用：**未做**。

## 发现的缺口（下次开工先修）
1. **未 commit/push**（最高优先，D 盘有 80% 误删史，唯一活源 GitHub main）。
2. **档案落后代码**：00_START_HERE 快照 / 05_TEST_LOG 已验证项 都没写 PersonalLexeme、剩余量、PARTIALLY_SHIPPED；03_VERBS 只写 3 个动词（实际 12）。接手 AI 会误判。
3. **order.query.json status enum = [DRAFT,CONFIRMED,SHIPPED,CANCELLED]**，缺 PARTIALLY_SHIPPED / SUPERSEDED →
   「查部分出货的单」会被 enum_alias 拦掉（resolve.ts 校验 meta.enum.includes）。
4. 无类型闸门：`npm run build` 是 vite build 不跑 tsc；手动 `npx tsc --noEmit` 通过（0 错误），未写进 scripts。

## 约定
- 本次为纯只读通读 + 顾问评审，未改动任何 HANDOVER 档案与源码。
- 评审落盘：`.workbuddy/reviews/2026-09-06-顾问评审.md`（非 HANDOVER 档案区）。

## 顾问评审核心结论（2026-09-06 晚，Owner 确认并行写入是另一个 agent）
- **P0-1 creditUsed 全库零写入**（只读 4 处：order.create:62 / credit.check:16,26 / customer.query:35）→
  信用额度是死数字；05_TEST_LOG「信用超额 block」是假阳性（靠 seed 王五 28000/30000 贴上限才触发）。
- **P0-2 订单永远单行**（order.create:165,192 单行 create）→ 多品种话术第二行静默丢失；
  delivery.create 的 items.length===1 当前恒真，靠单行约束兜底，非设计。
- **P1-3 预留与出货两套账**：delivery.confirm 只扣 qty 不动 reserved；预留无订单关联字段。
- **P1-4 用语表三缺陷**：proposeFromConfirm 不过滤 rejected（违反 11 号验收3）；
  ConfirmCard 记住是批量盲记整句+全槽位；similarity 单字 t.includes(q)→0.95 会命中（违反 len≥2 设计）。
- **P1-5 order.query.json status enum 缺 PARTIALLY_SHIPPED / SUPERSEDED**。
- ✅ 已核对无漏洞：order.cancel 对 PARTIALLY_SHIPPED 会走兜底分支被拒。
- 流程：smoke-verbs 零断言；无 typecheck 脚本；无鉴权；dev.db 无备份（setup 会清掉用语表）。
- 并行写入风险已建议：单一写者 / 分文件所有权 / 收口即 commit / 检查清单加「git status 必须干净」。
