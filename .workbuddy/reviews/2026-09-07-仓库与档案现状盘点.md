# Agent_ERP 仓库 + 档案现状盘点（2026-09-07 14:30）

> 方法：只读扫描。工作树 + `HANDOVER/` + `prisma/dev.db` 实况三方对照。
> 判定依据写「证据」列，不用感觉打分。

---

## 一句话结论

**后端与规则层已经完整且经过实跑验证；前端"人用过的痕迹"几乎为零；语音（ASR）只有 7KB 脚手架，零权重。**
最真实的缺口不是代码没写，是**没人真的用过**。

---

## A · 完整且已验证（可对外演示）

| 模块 | 证据 | 状态 |
|---|---|---|
| 12 动词 + 12 Schema | `src/server/verbs/` 12 个 + `src/schema/` 12 个，一一对应 | ✅ |
| 云端大脑 | `eval/BASELINE.md`：doubao-seed-2.0-lite，**动词 100% / 槽位 100%**，连通 1.7s | ✅ |
| 规则兜底 | 同表：规则 **96.0% / 98.3%**；无 Key 静默回落已实现 | ✅ |
| 订单多行 | DB：`Order` 8 行 / `OrderItem` 9 行 → 确实存在多行单 | ✅ |
| 部分出货 + 超量硬拦 | `order.query.json` enum 已含 `PARTIALLY_SHIPPED` + `SUPERSEDED` | ✅ |
| 信用额度闭环 | `order.confirm:60` increment、`order.cancel:50` 释放、`order.create:290` 变更单回补 | ✅ 已修 |
| 用语表 reject / 单字保护 | `lexicon.ts:312` 过滤 rejected；`:162/166` 双长度门槛 | ✅ 已修 |
| 评测四件套 | `eval` / `eval:lexicon` / `eval:compare` / `trial:10` | ✅ |
| typecheck | `package.json` 已加 `"typecheck": "tsc --noEmit"` | ✅ 已修 |
| smoke 断言版 | `smoke-multiline-reserve.ts` 27 处断言、`smoke-delivery-remaining.ts` 11 处 | ✅ 已修 |
| 错例库 | `eval/failures.jsonl` 存在 | ✅ |

> 注：上一轮评审提的 P0-1 / P1-4a / P1-4c / P1-5 / P1-7 五项，**已全部修复并 commit**（`5971f49`、`02e3d77`）。

---

## B · 代码完成，但从未真跑过 ⚠️（最大风险区）

| 模块 | 证据 | 判定 |
|---|---|---|
| **个人用语表** | `PersonalLexeme` 表存在但 **0 行** | 代码完工，零真实数据 |
| **画布面板** | `Panel` 表 **0 行** | UI 从未真正用过（或格子不落库，需确认设计） |
| **Owner 10 条真试用** | `trial:10` 脚本在，但跑过必然产生 lexicon 数据 → 推断**未跑** | 未验证 |
| **UI 点「记住」手感** | `05_TEST_LOG:40` 自述「API 已覆盖；UI 需浏览器」 | 未验证 |
| **本地服务** | `localhost:3001` → 502，服务未启动 | 未运行 |

**这一组是真正的短板：系统能跑，但没人验证过"人用起来顺不顺"。**

---

## C · 只有脚手架（目录建了，内容是空的）🚨

| 模块 | 应有 | 实际 |
|---|---|---|
| **本地 ASR 权重** | SenseVoice int8 ≈ 229MB | `models/asr/sensevoice/` 只有 `.gitkeep` |
| **热词表** | 全量客户/产品名 | `models/asr/hotwords.txt` **190 字节** |
| **本地 LLM** | Qwen3-0.6B ≈ 400MB | `models/llm/qwen3-0.6b/` 只有 `README.txt` |
| **models/ 总体积** | ≈ 229MB | **7.0 KB** |
| ASR 评测集 | ≥ 30 条 | `eval/asr-utterances.jsonl` **3 条** |
| CER 脚本 | 需权重才能跑 | `asr-cer.ts` 在，跑不了 |

**判定：语音本地化（#26 的当前最高优先级）实际进度 = 0%。**
`npm run hotwords` / `npm run asr:cer` 现在跑都是空转。

---

## D · 未开始 / 明确不做

| 项 | 归属 |
|---|---|
| PostgreSQL、并发压测、内存泄漏 | `05_TEST_LOG` 未验证清单，P3 |
| VLM / 多模态实现 | 决策：接口预留，实现延后 |
| 采购 / 财务 / 权限 / 多租户 | 红线第 5 条，明确不做 |
| `price.query` | 备选，明确不做 |
| mini apps 并入 | D1 决策：S5 之后 |

---

## E · 档案与代码不一致（3 处，需改）

| # | 位置 | 问题 | 应改为 |
|---|---|---|---|
| 1 | `06_ROADMAP.md` 二·W1 表 | P0「跑模型 vs 规则对比」仍写「规则基线已出；**模型待 Key**」 | 模型已跑完 100%/100%，改 ✅ |
| 2 | `06_ROADMAP.md` 二·W1 表 | P1「错例落库」标 ✅，但 `failures.jsonl` 仅 1.3KB | 保留 ✅，补一句样本量 |
| 3 | `00_START_HERE.md` 状态快照 | 「ASR 热词/CER **脚手架**」标 ✅ | 措辞诚实，但建议加「**零权重，未实跑**」避免误判 |

---

## F · 工作区未提交（10 改 + 4 新增）

```
 M .gitignore                       ?? .workbuddy/            （本顾问 3 份评审）
 M HANDOVER/00_START_HERE.md        ?? app/eval/asr-utterances.jsonl
 M HANDOVER/01_VISION.md            ?? app/models/
 M HANDOVER/04_DECISIONS.md         ?? app/scripts/asr-cer.ts
 M HANDOVER/06_ROADMAP.md           ?? app/scripts/export-hotwords.ts
 M HANDOVER/08_FILE_TREE.md
 M app/eval/COMPARE_MODELS.md
 M app/package.json
 M app/src/server/settings.ts
 M app/src/ui/SettingsModal.tsx
```

⚠️ 违反 `06_ROADMAP` 检查清单第 0 步「动工前 git status 干净」。
好消息：另一 agent 已收口提交 4 次（`134af10` → `02e3d77`）。

---

## G · 建议的下一步（按性价比排序）

| P | 动作 | 成本 |
|---|---|---|
| **P0** | 提交当前工作区（含 `.workbuddy/` 评审） | 2 分钟 |
| **P0** | 启动服务，Owner 用画布亲手跑 10 条真口吻（这是 B 组唯一的解药） | 30 分钟 |
| **P0** | 拉 SenseVoice 权重（229MB）—— 否则 #26 语音本地化一直是 0% | 下载时间 |
| P1 | 修 E 组 3 处档案不一致 | 10 分钟 |
| P1 | `npm run hotwords` 重新导出全量热词（现 190B 明显不全） | 1 分钟 |
