# HANDOVER 档案补写清单（可直接派工）

> 出具人：顾问评审 · 2026-09-07 00:25
> 核对基准：`git` 工作树实况（`f1eaa81` + 20 改 11 新增未提交）
> 原则：**只补事实，不重写风格**；每条都写明「改哪 → 改成什么 → 怎么验收」

---

## 执行前铁律（第 0 步）

```bash
cd D:/Development/Agent_ERP
git status --short        # 必须看清当前 20M + 11?? 都是你的
git stash list            # 确认无别人暂存
```

⚠️ **同一时刻只保留一个写者**。若另一会话正在改 `app/src/server/`，本清单只做 `HANDOVER/` 的文档改动，两不冲突。
改完 `HANDOVER/` 后**立即单独 commit**（`docs: sync HANDOVER to 2026-09-07 code reality`），不要和代码改动混在一个提交里。

---

## A · `03_VERBS.md` —— P0，最大缺口

**现状**：标题「一、已实现的 3 个动词」，实际 12 个。接手的 AI 读完会以为项目停在 3 动词阶段。

### A1 第一节：3 → 12（第 5 行起）

标题改为「一、已实现的 12 个动词」，原表保留 3 行，补 9 行：

| 动词 | 风险 | 必填 | 说明 |
|---|---|---|---|
| `order.cancel` | write | `orderNo` | 取消；已出货/部分出货走兜底拒绝 |
| `delivery.create` | write | `orderNo` | 建出货单；**受订单行剩余量约束** |
| `delivery.confirm` | write | `deliveryNo` | 确认出货 → 扣库存；订单转 `PARTIALLY_SHIPPED`/`SHIPPED` |
| `delivery.query` | read | 无 | 查出货单 |
| `inventory.query` | read | 无 | 查库存（含 `reserved`） |
| `inventory.reserve` | write | `productId` `qty` | 预留；增 `reserved` |
| `inventory.release` | write | `productId` `qty` | 释放预留 |
| `customer.query` | read | 无 | 查客户主数据 + 信用 |
| `credit.check` | read | `customerId` | 信用检查（**只读判断，不占用**，见已知限制） |

### A2 第二节：业务规则表补 2 行（第 35 行起）

在现有 `credit_limit / min_order_amount / price_floor / inventory` 后追加：

| 规则 | 严重度 | 触发条件 | 结果 |
|---|---|---|---|
| `shipping_over` | **block** | 本次出货量 > 订单行剩余可出货量 | 拒绝出货（硬拦，不可 confirm） |
| `shipping_partial` | **auto** | 0 < 出货量 < 订单行总量 | 订单置 `PARTIALLY_SHIPPED`，可继续出货 |

并在表下补一句：> 剩余量计算见 `app/src/server/remaining.ts`，被 `delivery.create` / `delivery.confirm` 共用。

### A3 新增一节：订单状态机（放在第三节变更单机制之前）

```
DRAFT --confirm--> CONFIRMED --delivery.confirm(部分)--> PARTIALLY_SHIPPED
                                    |                          |
                                    +--(全额)--> SHIPPED <------+ (补足后)
DRAFT / CONFIRMED --cancel--> CANCELLED
CONFIRMED / PARTIALLY_SHIPPED --变更单--> SUPERSEDED
```

⚠️ **已知待修**：`app/src/schema/order.query.json` 第 28 行 `status` enum 仅
`[DRAFT, CONFIRMED, SHIPPED, CANCELLED]`，**缺 `PARTIALLY_SHIPPED` 与 `SUPERSEDED`**
→「查部分出货的单」会被 `enum_alias` 判非法值拦掉。改状态机枚举时**必须同步此处**。

### A4 第五节：API 路由表补 5 条（第 105 行起）

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/lexicon` | 列出个人用语（可按 kind / scope 过滤） |
| POST | `/api/lexicon` | 写入/更新一条用语（`source=explicit\|proposed`） |
| DELETE | `/api/lexicon/:id` | 删除一条用语 |
| POST | `/api/lexicon/:id/reject` | 拒绝提议（置 `status=rejected`，**不再复弹**） |
| POST | `/api/lexicon/:id/hit` | 命中计数 +1 |

> 实现在 `app/src/server/index.ts:152-228`。

### A5 新增「已知限制」小节（文件末尾）

三条，照抄：

1. **`order.create` 单行**：`order.create.ts:165/192` 均为单行 `create:[{...}]`。
   「100 个 A-100 和 200 个 B-200」→ 第二行**静默丢弃**。多行支持未做。
2. **`creditUsed` 无写入点**：全库零处 update，唯一来源是 seed。
   `credit_limit` 规则目前只对种子里已贴近上限的客户生效。
3. **预留与出货两套账**：`delivery.confirm` 只扣 `qty` 不动 `reserved`；
   预留无字段指向订单，答不出「这 50 个给谁留的」。

### A6 验收

- `grep -c "order\.\|delivery\.\|inventory\.\|customer\.\|credit\." 03_VERBS.md` ≥ 12 个动词名
- 文件中不再出现「已实现的 3 个动词」
- `/api/lexicon` 5 条路由在表内

---

## B · `05_TEST_LOG.md` —— P0，含一条假阳性

### B1 修正第 6 条（一、已验证表格）

原：`6 | 业务规则 · 信用额度 | 超额下单 | block，拒绝建单`
改为：

| 6 | 业务规则 · 信用额度 | 超额下单 | ⚠️ **假阳性**：`creditUsed` 全库零写入点，该次能触发仅因种子 `王五 28000/30000` 贴近上限。真实场景连下多单**不会**被拦 |

### B2 修正第 12 条

原：`12 | 3 动词注册`
改为：`12 | 12 动词注册 | GET /api/verbs | 12 个动词，参数与必填正确`

### B3 第四节「数据库当前状态」补三行

| 表 | 数量 | 备注 |
|---|---|---|
| Delivery | 待测 | `npm run db:push && npm run db:seed` 后记录 |
| PersonalLexeme | 0 | 用语表；`eval:lexicon` 跑完应回 0（自动清理） |
| Inventory.reserved | 见 seed | 与出货扣减**不同步**，见 A5-3 |

### B4 「未验证」清单补三行

| 多行订单 | `order.create` 仅单行 | 见 A5-1 |
| 预留↔出货联动 | `reserved` 不随 confirm 减少 | 见 A5-3 |
| 并发写入 | 曾出现两会话同改 `server/` | 定单一写者后重测 |

### B5 验收

第 6 条带「假阳性」字样；第 12 条为 12；第四节表 ≥ 6 行。

---

## C · `02_ARCHITECTURE.md` —— P1

### C1 第四节「数据模型」（第 124 行起）补 `PersonalLexeme`

补字段表（`schema.prisma:106`）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` / `createdAt` / `updatedAt` | — | 常规 |
| `kind` | enum | `verb` / `slot` |
| `scope` | string | slot 用语的槽位名（`customer` / `product` / …） |
| `phrase` / `phraseNorm` | string | 原话 / 归一化（去空格、小写、全半角） |
| `target` | string | verb 名 或 目标实体 ID |
| `source` | enum | `explicit` / `proposed` / `seed` |
| `status` | enum | `active` / `rejected` |
| `hits` | int | 命中计数 |

同时补 `Delivery` / `DeliveryLine` / `Inventory.reserved` 是否存在于当前 schema（核对后写）。

### C2 补一小节「个人用语表的两个插入点」

- **插入点 A（动词）**：`agent.ts:373-385`，`detectVerb` 之前查 `kind=verb`，命中 conf 0.95
- **插入点 B（槽位）**：`resolve.ts:370-410`，`fuzzy_customer` / `fuzzy_product` / `enum_alias` 之前查 `kind=slot`；target 失效自动 retire

> 完整契约见 `11_PERSONAL_LEXICON.md`。

### C3 验收

`grep -c PersonalLexeme 02_ARCHITECTURE.md` ≥ 3。

---

## D · `08_FILE_TREE.md` —— P1

现状：已含 `server/lexicon.ts` / `server/remaining.ts`（29-30 行），**缺 8 个新增文件**。补：

| 路径 | 说明 |
|---|---|
| `scripts/eval-lexicon.ts` | 用语夹具评测（跑完自动清理，不污染基线） |
| `scripts/eval-compare-models.ts` | 云端 vs 本地对比，走 `LOCAL_LLM_*` |
| `scripts/smoke-delivery-remaining.ts` | 出货剩余量冒烟（**有断言**） |
| `scripts/trial-10-utterances.ts` | Owner 10 条真口吻 API 链路 |
| `eval/utterances.lexicon.jsonl` | 用语夹具 3 条 |
| `eval/COMPARE_MODELS.md` | 对比说明 |
| `Start.bat` | 一键启动（仓库根） |
| `../HANDOVER/11_PERSONAL_LEXICON.md` | 用语表设计 |

⚠️ 顺带修正：`scripts/smoke-verbs.ts` 标注为「**仅 console.log，零断言**」，不可作为回归依据；回归以 `smoke-delivery-remaining.ts` 为模板。

---

## E · `04_DECISIONS.md` —— P1

### E1 决策表（第 24 行区）补 3 条

| 14 | 出货超量 | **硬拦 block**，不 warn 不 confirm | 放行后补录 |
| 15 | 部分出货 | 新增 `PARTIALLY_SHIPPED` 状态，可多次出货至补足 | 强制一次出完 / 拆成多张单 |
| 16 | 订单行 | 当前**只支持单行**（已知限制，暂不做多行） | — |

### E2 第六节「备份与版本控制」更新

- 「未完成：GitHub 远程仓库」→ **已完成**：`EnglandTong/Agent_ERP`，已推 3 次提交（`f1eaa81` 最新）
- 沙箱网络段保留（仍然真实）
- 补：当前有 **20 个已改 + 11 个新增未提交**，唯一活源仍是本地工作树 —— 推送前本地事故无法恢复

### E3 新增第八节「并行写入纪律」

1. 同一时刻只保留一个写者（只读评审不冲突）
2. 必须并行 → 分文件所有权（如 A 管 `src/server/`、B 管 `src/ui/` + `eval/`），不得交叉
3. 收口即 commit，message 标来源
4. 加能力检查清单第 0 步：**动工前 `git status` 必须干净**

---

## F · `06_ROADMAP.md` —— P2

### F1 「加能力检查清单」（第 29 行起）补 3 步

- 第 0 步：`git status` 必须干净
- 第 5 步之后：`npx tsc --noEmit`（注意 `npm run build` 是 vite build，**不做类型检查**）
- 第 6 步之后：**涉及状态机的动词，同步检查所有 `status` enum**（`order.query.json` 漏项即此）

### F2 第五节「已知风险与开放问题」补 5 条

引用本评审：信用死数字 / 单行订单 / 预留两套账 / 用语表三缺陷 / status enum 缺项。

---

## G · `00_START_HERE.md` —— P2

1. 档案目录表：`03_VERBS.md` 描述「3 个动词规格」→「**12 个动词规格 + 订单状态机 + 已知限制**」
2. 第四节接手后第一步：P0 从「填 API Key」前插一条
   **P0-0 | 提交当前 20 改 11 新增 + push | `git status` 干净**

---

## 执行顺序建议（一个 agent 一轮做完，约 40 分钟）

| 序 | 文件 | 优先级 | 依赖 |
|---|---|---|---|
| 1 | `03_VERBS.md` A1–A5 | P0 | 无 |
| 2 | `05_TEST_LOG.md` B1–B4 | P0 | 无 |
| 3 | `04_DECISIONS.md` E1–E3 | P1 | 无 |
| 4 | `02_ARCHITECTURE.md` C1–C2 | P1 | 需读 `schema.prisma` 核对 Delivery 模型 |
| 5 | `08_FILE_TREE.md` D | P1 | 无 |
| 6 | `06_ROADMAP.md` F1–F2 | P2 | 引用 1–2 的结论 |
| 7 | `00_START_HERE.md` G | P2 | 最后做，与 1 保持一致 |
| 8 | `git commit -m "docs: sync HANDOVER to code reality (12 verbs, lexicon, remaining)"` | — | 全部完成 |

## 不建议现在做的

- ❌ 不要在补档案的同时改代码（会与并行 agent 打架）
- ❌ 不要为「让档案好看」而删历史决策段落 —— `04_DECISIONS` 的「被否决方案」是有价值的负知识
- ❌ 不要新建 12 号、13 号档案 —— 事实源只有一个 `HANDOVER/`，新内容进既有编号
