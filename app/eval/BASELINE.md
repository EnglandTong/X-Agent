# 评测基线 · 模型 vs 规则 · 2026-09-06

> 由 `npm run eval` 生成。模型：`doubao-seed-2.0-lite` @ 火山方舟 coding v3。

| 指标 | 规则 | 模型 |
|---|---|---|
| 样本数 | 50 | 50 |
| 动词准确率 | **96.0%** | **100.0%** |
| 槽位命中率 | **98.3%** | **100.0%** |
| 全对（OK） | 46 | — |
| Tag D（模型对、规则错） | — | **4** |
| Tag A/B/C | 0 | 0 |

## 结论

- **当前默认走云端模型是合理的**：本评测集上模型全面不低于规则。
- **规则仍必须保留作兜底**：无 Key / 超时 / 401 时静默回落（已实现）。
- Tag D 的 4 条是规则弱点、模型补上的场景（口语产品名、客户编码、部分查询口吻等）—— 正好说明「互补」架构有价值。

## 连通性（本次实测）

- `POST /api/settings/test` → `ok: true` ·约 1.7s
- 引擎：`doubao-seed-2.0-lite`

## 怎么复跑

```bash
cd app
npm run eval
```
