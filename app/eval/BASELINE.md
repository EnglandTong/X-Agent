# 评测基线 · 模型 vs 规则 · 2026-09-08（T4 后重跑）

> 由 `npm run eval` 生成。模型：`doubao-seed-2.0-lite` @ 火山方舟 coding v3。
> 上一版（2026-09-06）写的是规则槽位 98.3% / OK 46 / Tag D 4 —— 与实测不符，已按本次结果更正。

| 指标 | 规则 | 模型 |
|---|---|---|
| 样本数 | 50 | 50 |
| 动词准确率 | **96.0%** | **100.0%** |
| 槽位命中率 | **95.0%** | **100.0%** |
| 全对（OK） | 42 | — |
| Tag D（模型对、规则错） | — | **8** |
| Tag A/B/C | 0 | 0 |

## 结论

- **默认走云端模型是合理的**：本评测集上模型全面不低于规则。
- **规则仍必须保留作兜底**：无 Key / 超时 / 401 时静默回落（已实现）。
- **规则档的 8 条弱点**集中在：中文数字数量、口语化产品名、部分查询口吻 —— 正好说明「互补」架构有价值。

## ⚠️ 另一把尺子：真口吻（39 条，见 `npm run eval:asr`）

评测集写得太规整，会高估规则档。同一套代码在真口吻样本上：

| 档 | 动词准确率 |
|---|---|
| 云端 | **97.3%** |
| 规则 | **78.4%**（比上表的 96.0% 低 17.6 个百分点） |

规则档分类短板：`warehouse 25%` < `mixed 60%` < `qty/date 83%` < `customer 86%`。
**出货类口吻在规则档 4 条全错** —— 断网可开单，但"出货"不能宣传成可离线。

## 连通性（2026-09-08 实测）

- `POST /api/settings/test` → `ok: true`
- 引擎：`doubao-seed-2.0-lite`

## 怎么复跑

```bash
cd app
npm run eval                  # 规整样本 50 条（两档）
npm run eval:asr              # 真口吻 39 条（云端档）
set LLM_PROVIDER=rules&& set PORT=3002&& npm run serve
BASE_URL=http://127.0.0.1:3002 npm run eval:asr   # 真口吻（规则档）
```
